import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tanmay Hatkar — ML Engineer & Team Lead",
  description:
    "ML Engineer and Team Lead building LLM-powered applications, autonomous AI agents, and production ML systems. Published researcher (ICMVA 2025).",
  openGraph: {
    title: "Tanmay Hatkar — ML Engineer & Team Lead",
    description:
      "ML Engineer and Team Lead building LLM-powered applications, autonomous AI agents, and production ML systems.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
      </body>
    </html>
  );
}
